<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= recipe.name %></title>
    <style>
        .ingredient {
            position: relative;
            display: inline-block;
            cursor: pointer;
            margin: 5px;
            padding: 2px 4px;
            background-color: #f0f0f0;
            border-radius: 4px;
        }
        .ingredient .tooltip {
            visibility: hidden;
            width: 200px;
            background-color: black;
            color: #fff;
            text-align: center;
            padding: 5px;
            border-radius: 6px;
            
            position: absolute;
            z-index: 1;
            bottom: 125%; /* place above */
            left: 50%;
            margin-left: -100px;
            opacity: 0;
            transition: opacity 0.3s;
        }
        .ingredient:hover .tooltip {
            visibility: visible;
            opacity: 1;
        }
    </style>
</head>
<body>
    <h1><%= recipe.name %></h1>

    <p><%= recipe.description %></p>

    <h2>Ingredients:</h2>
    <ul>
        <% ingredients.forEach(ingredient => { %>
            <li class="ingredient">
                <%= ingredient.name %>
                <div class="tooltip"><%= ingredient.fact %></div>
            </li>
        <% }); %>
    </ul>

    <a href="/recipes">← Back to Recipe List</a>
</body>
</html>
